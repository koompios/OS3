# Maintainer: Hangsiahong <honghangsia@gmail.com>
pkgname=koompi-skel
pkgver=20240417
pkgrel=7
pkgdesc="KOOMPI OS Skel"
arch=('any')
license=('GPL')
package() {
    # Use the 'install' command to copy files and overwrite existing files if they exist
    mkdir -pv $pkgdir/etc/
    cp -r ../koompi-skel/* $pkgdir/
   # install -m 644 ../koompi-skel/etc/skel/.bash_profile "$pkgdir/etc/skel/.bash_profile"
   # install -m 644 ../koompi-skel/etc/skel/.bashrc "$pkgdir/etc/skel/.bashrc"
}
