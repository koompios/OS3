pkgname='wpm'
pkgver=0.1.0
pkgrel=3
pkgdesc='Waydroid Package Manager'
arch=(any)
#source=('wpm::git+https://github.com/koompi-os/waydroid-package-manager.git')
sha256sums=('SKIP')
depend=('android-tools' 'xmlstarlet')

package() {
    mkdir -pv $pkgdir/usr/
    cp -pr ../wpm/* $pkgdir/
}
